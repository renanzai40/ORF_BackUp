[project]
name = "omni-re-formatter"
version = "0.2.0"
description = "Omni-Re-Formatter: 将本地化后的 MD/XLIFF 还原为目标复杂格式"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "1StepMore" }
]
keywords = ["localization", "docx", "md", "xliff", "pandoc"]
classifiers = [
    "Development Status :: 1 - Planning",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "click>=8.1.0",
    "tqdm>=4.66.0",
    "pyyaml>=6.0",
    "python-magic>=0.4.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
    "mypy>=1.5.0",
    "weasyprint>=60.0",
    "boto3>=1.34.0",
    "azure-storage-blob>=12.19.0",
    "openai>=1.12.0",
]
test = ["coverage"]
weasyprint = ["weasyprint>=60.0"]
cloud = ["boto3>=1.34.0", "azure-storage-blob>=12.19.0"]
ai = ["openai>=1.12.0"]
office = ["openpyxl>=3.0.0"]
notebook = ["nbformat>=5.0.0"]
email-output = ["aspose-email-foss>=24.0.0"]
mcp = ["fastmcp>=0.1.0"]

[project.scripts]
orf = "orf.cli:main"
orf-mcp-server = "orf.mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orf"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "e2e: End-to-end tests",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
# strict = true  # Disabled until type errors are fixed across codebase